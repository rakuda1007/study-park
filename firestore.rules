rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 開発初期はすべて拒否。コレクション設計と認証ルールを決めてから match を追加してください。
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
